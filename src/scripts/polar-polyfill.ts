// Polyfill for @convex-dev/polar
// This file serves as a workaround for the missing polyfill export in @convex-dev/polar package

// The original polyfill is minimal, so we'll create a compatible one
// Based on the actual polyfill content from @convex-dev/polar

export {};
